---@class VinLib
local M = {
    copy = require("vin.lib.copy"),
    colors = require("vin.lib.colors"),
    files = require("vin.lib.files"),
    git = require("vin.lib.git"),
    ui = require("vin.lib.ui"),
}

return M
